[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pausiva-agent"
version = "1.0.0"
description = "Sistema multiagente de acompañamiento para Pausiva"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=1.0.0",
]

[project.optional-dependencies]
supabase = [
    "supabase>=2.0.0",
]
dev = [
    "pytest>=7.0.0",
    "supabase>=2.0.0",
]
all = [
    "supabase>=2.0.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["pausiva_agent*"]
